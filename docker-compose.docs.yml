services:
  docs:
    image: the_wall_api_local_docs
    container_name: the_wall_api_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./the_wall_api:/app/the_wall_api:z
    ports:
      - '9000:9000'
    command: /start-docs
